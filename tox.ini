[tox]
envlist = py311
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = False
source = ./app/
branch = True